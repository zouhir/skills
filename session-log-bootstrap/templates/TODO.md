# TODO

Ordered task list for this project. The agent reads this top-to-bottom and works on the first unchecked item when asked for "next task". Keep tasks small enough to finish in one session (roughly 30 min – 2 hours of agent work). Break big things down before adding them here.

## Format

- `- [ ]` = not started
- `- [x]` = done (the agent checks these off; do not delete the lines)
- One task per line. Title should be specific enough that the agent knows what "done" means.
- Order = priority. Top of the list gets worked on first.

## Tasks

- [ ] Example: Add a `/health` endpoint that returns `{status: "ok"}` and a 200
- [ ] Example: Write a unit test for the existing `parseConfig()` function covering the three error cases
- [ ] Example: Document the deployment flow in `docs/deploy.md`

<!-- Add new tasks below. Delete the examples once you have real ones. -->
